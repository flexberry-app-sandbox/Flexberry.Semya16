import { Serializer as ТемпаУстанSerializer } from
  '../mixins/regenerated/serializers/i-i-s-semya16-темпа-устан';
import __ApplicationSerializer from './application';

export default __ApplicationSerializer.extend(ТемпаУстанSerializer, {
  /**
  * Field name where object identifier is kept.
  */
  primaryKey: '__PrimaryKey'
});
